# ترويج بوت v0.9.3: المرحلة 6 (تسابقات حالة الطلبات)

الفرع: `fix/phase6-races`، مبني فوق `fix/phase3-5` (v0.9.2 بمراحلها الخمس). الدليل السابق `TESTING_v0.9.2.md` ما زال صالحاً لما قبل هذه المرحلة.
تقرير المراجعة الذي كشف المشاكل: `tests/_sim/review2/REVIEW2_REPORT.md`.

## ⚙️ قبل التشغيل
- **ترحيل جديد:** `app/db/migrations/013_submit_claim.sql` يُطبَّق تلقائياً عند الإقلاع.
  - يضيف عموداً واحداً هو `orders.submitting_until`، ولا يعدّل أي بيانات.
- **بلا متغيرات بيئة جديدة.**
- **ارفع الملفين** `.env.example` و`.gitignore` يدوياً إلى GitHub؛ هما غير موجودين هناك، لأن السحب والإفلات يتجاهل الملفات التي يبدأ اسمها بنقطة.

## ✅ ما الذي تغيّر

| المشكلة | كانت النتيجة | الآن |
|---|---|---|
| استرداد الأدمن أثناء إرسال الطلب إلى نور | العميل يسترد **والحملة تعمل** | الاسترداد يُرفض برسالة «⏳ قيد الإرسال — أعد بعد دقيقة» |
| إرسال الطلب نفسه إلى نور من مكانين معاً | طلبا HTTP (حملتان إن لم تحترم نور المفتاح) | طلب واحد فقط |
| إلغاء العميل ⟷ نشر أو بدء الأدمن | استرداد كامل مع منشور منشور (4/30) | ينجح طرف واحد فقط |
| الاعتماد التلقائي ⟷ تعديل مدفوع | رسم مخصوم والطلب مغلق (39/40) | التعديل يفوز |
| مزامنة نور بعد استرداد الأدمن | قد تعيد فتح الطلب | لا تكتب فوق الاسترداد |
| 3 أخطاء شبكة متتالية | استرداد تلقائي حتى لو أُنشئت الحملة | نتحقق من نور أولاً؛ إن تعذّر التحقق يبقى الطلب معلّقاً ويُنبَّه الأدمن |
| استرداد حملة Meta موجودة عند نور | بلا تنبيه | تحذير «أوقفها من لوحة نور» في شاشة الاسترداد |

## 🧪 تجربة يدوية سريعة (نحو 10 دقائق)
1. **الاسترداد والإرسال (وضع DRY RUN):** اطلب إعلان Meta، ومن بطاقة الأدمن اضغط «↩️ استرداد».
   - يجب أن يظهر تحذير «الحملة موجودة عند Nour Ads» إن كان للطلب `nour_id`.
   - اكتب السبب، فيجب أن يُسترد المبلغ ويظهر في «سجل الرصيد» مرة واحدة.
2. **إلغاء منشور شريك:** اطلب منشوراً في قناة شريكة، وافتح الطلب في حساب العميل وبطاقة الأدمن معاً.
   - اضغط «🚫 إلغاء» عند العميل و«🔗 تم النشر» عند الأدمن في وقت متقارب.
   - المتوقع: ينجح أحدهما فقط، ويرى الآخر «تغيّرت حالة الطلب».
3. **تعديل التصميم:** سلّم تصميماً، ثم من حساب العميل اضغط «✏️ تعديل» و«✅ اعتماد» بسرعة.
   - المتوقع: إما تعديل (مع خصم الرسم إن لم يكن مجانياً) أو اعتماد، ولا يجتمعان.

## 🔬 الاختبارات الآلية
```bash
bash tests/_sim/explore/setup_env.sh
TEST_DATABASE_URL=postgresql://postgres@127.0.0.1:5433/promobot /tmp/venv/bin/python -m pytest -q tests/
bash tests/_sim/explore/run_all.sh 800
# سكربتات إعادة الإنتاج (كانت تكشف المشاكل، والآن تُظهر ✅):
for f in tests/_sim/review2/repro_*.py; do /tmp/venv/bin/python $f; done
```
آخر نتيجة:
- **pytest:** 90/90 (ثلاث مرات متتالية). منها 12 اختباراً جديداً في `tests/test_races_orders.py`، **5 منها تفشل على الكود القديم** وتنجح على الجديد.
- **المحاكاة:** 800 خطوة، **0 مشاكل**، و0 فروقات بين الرصيد والسجل.
- **المحاكاة الأصلية:** 10/10 (أثر الخطأ في `sim_channels` هو «test boom» المقصود).
- **سكربتات إعادة الإنتاج الثلاثة:** 0 تعارضات، وطلب HTTP واحد إلى نور.
- **ruff (أخطاء F):** نظيف.

## 🚀 النشر
الخطوات نفسها كما في `TESTING_v0.9.2.md`: نسخة احتياطية من Neon، ثم بوت تجريبي، ثم رفع الملفات. الترحيل `013` يُطبَّق وحده.
