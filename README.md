# 🤖 ترويج بوت — الخطوة 3 من 6 (+ إعلانات تيليغرام)

بوت تيليغرام لبيع خدمات الترويج (إعلانات فيسبوك/إنستغرام عبر Nour Ads، Telegram Ads، قنوات شريكة، تصميم وكتابة).
مبني بـ Python + aiogram 3، قاعدة بيانات Neon Postgres، استضافة Render (خطة مجانية)، وإبقاء بـ UptimeRobot.

> **ما الذي يعمل حتى الآن؟** الترحيب والشروط، القائمة الرئيسية الملوّنة، الرصيد والشحن (USDT + شام كاش) مع اعتماد الأدمن،
> **معالج إعلان فيسبوك/إنستغرام كاملاً** (باقات + مخصص + حزمة انطلاقة متجر) مع الخصم من الرصيد وبطاقة الأدمن ووضع محاكاة نور 🧪،
> «📦 طلباتي»، لوحة الأدمن، `/health` لـ UptimeRobot. الإرسال الحقيقي إلى Nour Ads يأتي في الخطوة 4.

---

## 0) ما تحتاجه قبل البدء (10 دقائق)

| | من أين | ما الذي تنسخه |
|---|---|---|
| توكن البوت | [@BotFather](https://t.me/BotFather) ← `/newbot` | `123456789:AAE...` — **ابدأ ببوت تجريبي** واحتفظ بالرسمي للنهاية |
| معرّفك الرقمي | أرسل `/id` لأي بوت يعمل من هذا الكود، أو [@userinfobot](https://t.me/userinfobot) | رقم مثل `123456789` |
| قاعدة البيانات | [neon.tech](https://neon.tech) ← New Project | زر **Connect** ← اختر **Pooled connection** ← انسخ الرابط كاملاً |
| حساب GitHub | [github.com](https://github.com) | مستودع **خاص** (Private) باسم `promo-bot` |
| حساب Render | [render.com](https://render.com) — سجّل بحساب GitHub | لا يحتاج بطاقة |

---

## 1) رفع الكود إلى GitHub

**الطريقة السهلة (بدون أوامر):** أنشئ المستودع الخاص على GitHub ← زر **Add file ← Upload files** ← اسحب محتويات مجلد `promo-bot` كاملاً (المجلدات `app` و`tests` والملفات `requirements.txt` `render.yaml` `.env.example` `.gitignore` `README.md`) ← **Commit**.

**بالأوامر (ويندوز PowerShell أو ماك/لينكس):**
```bash
cd promo-bot
git init
git add .
git commit -m "step 1: skeleton, db, main menu"
git branch -M main
git remote add origin https://github.com/<اسمك>/promo-bot.git
git push -u origin main
```

> ⚠️ لا ترفع ملف `.env` أبداً (هو في `.gitignore` أصلاً). الأسرار تُدخل في Render فقط.

---

## 2) إنشاء الخدمة على Render

1. **New ← Web Service** ← اختر مستودع `promo-bot`.
2. املأ:
   - **Name:** `promo-bot` (سيصبح رابطك `https://promo-bot.onrender.com` — أو أي اسم متاح)
   - **Region:** Frankfurt (الأقرب)
   - **Runtime:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python -m app.main`
   - **Instance Type:** **Free**
3. **Environment ← Add Environment Variable** — أضف هذه بالضبط:

   | Key | Value |
   |---|---|
   | `BOT_TOKEN` | توكن BotFather |
   | `DATABASE_URL` | رابط Neon المجمّع (Pooled) كاملاً |
   | `ADMIN_IDS` | معرّفك الرقمي (وأكثر من واحد بفواصل: `111,222`) |
   | `MODE` | `webhook` |
   | `WEBHOOK_SECRET` | سلسلة عشوائية طويلة من أحرف وأرقام فقط، مثل `k7Qp2ZxW9mL4tR8vB3nH6yJ1` |
   | `TZ` | `Asia/Damascus` |
   | `NOUR_DRY_RUN` | `1` (محاكاة — يصبح `0` في الخطوة 4) |
   | `BOT_NAME` | `ترويج بوت` |
   | `SUPPORT_USERNAME` | يوزر حساب الدعم بدون @ (اختياري) |

   > لا تحتاج `PUBLIC_URL`: Render يوفّر الرابط تلقائياً في `RENDER_EXTERNAL_URL` والكود يقرؤه.
   > (بديل: **New ← Blueprint** يقرأ `render.yaml` وينشئ كل شيء، ثم تدخل الأسرار الثلاثة فقط.)

4. **Create Web Service** ← انتظر 2–3 دقائق حتى ترى في السجل (Logs):
   ```
   INFO db: Postgres pool ready
   INFO db: migration applied: 001_init.sql
   INFO main: webhook set: https://promo-bot.onrender.com/webhook/...
   INFO main: bot @اسم_البوت started — v0.2.3 step 1/6 mode=webhook
   ```
5. ستصلك رسالة على تيليغرام من البوت: **«✅ ترويج بوت انطلق»** ← افتح البوت واضغط **Start** 🎉
   (إذا لم تصل الرسالة الأولى: اضغط Start في البوت أولاً ثم أعد النشر — تيليغرام لا يسمح للبوت بمراسلتك قبل أن تراسله.)

---

## 3) إبقاء البوت مستيقظاً — UptimeRobot (دقيقتان)

الخطة المجانية في Render تُنيم الخدمة بعد 15 دقيقة بلا زيارات، فيتأخر أول ردّ نصف دقيقة.

1. [uptimerobot.com](https://uptimerobot.com) ← **New Monitor**
2. Type: **HTTP(s)** · URL: `https://promo-bot.onrender.com/health` · Interval: **5 minutes**
3. أضف بريدك للتنبيه إن سقط البوت.

`/health` لا يلمس قاعدة البيانات عمداً — حتى تنام Neon وتبقى ضمن 100 ساعة-حوسبة الشهرية.

---

## 4) التشغيل على جهازك (اختياري — للتجربة قبل النشر)

```bash
cd promo-bot
python -m venv .venv
# ويندوز:  .venv\Scripts\activate      ماك/لينكس:  source .venv/bin/activate
pip install -r requirements.txt
copy .env.example .env      # ماك/لينكس: cp .env.example .env
# افتح .env واملأ BOT_TOKEN و DATABASE_URL و ADMIN_IDS  (MODE=polling)
python -m app.main
```
> استخدم **بوتاً ثانياً** (dev) للتجربة المحلية — البوت الواحد لا يعمل بوضعَي polling وwebhook معاً.
> ولقاعدة معزولة: في Neon أنشئ **Branch** باسم `dev` وضع رابطها في `.env`.

الاختبارات (بلا شبكة ولا قاعدة):
```bash
pip install pytest
python -m pytest -q
```

---

## 5) هيكل المشروع

```
promo-bot/
├── app/
│   ├── main.py               # الإقلاع: polling محلياً / webhook على Render + خادم /health
│   ├── config.py             # قراءة متغيرات البيئة والتحقق منها برسائل واضحة
│   ├── web.py                # /  و /health (بلا DB) و مسار الويبهوك
│   ├── db/
│   │   ├── pool.py           # اتصال Neon الصغير + إعادة محاولة + الترحيلات التلقائية
│   │   ├── migrations/001_init.sql   # 12 جدولاً: users, ledger, topups, orders, tasks, channels, tickets, settings, fsm_state, events…
│   │   └── repo/             # users.py, settings.py, events.py
│   ├── bot/
│   │   ├── texts.py          # كل النصوص العربية
│   │   ├── keyboards.py      # كل الأزرار — مطابقة لخريطة الأزرار
│   │   ├── fsm_storage.py    # حالة المعالج في Postgres (تصمد أمام إعادة التشغيل)
│   │   ├── middlewares.py    # تسجيل المستخدم، الحظر، التقاط الأخطاء
│   │   └── handlers/         # start.py (H0/H1) · menu.py (M0 T0 D0 B0 O0 S0 I0) · admin/panel.py (A0) · fallback.py
│   └── services/pricing.py   # جدول الأسعار — مصدر واحد للحقيقة
├── tests/                    # اختبارات التسعير والإعدادات
├── requirements.txt · render.yaml · .env.example · .gitignore
```

---

## 6) أوامر البوت

| للمستخدم | للأدمن |
|---|---|
| `/start` القائمة · `/balance` رصيدي · `/orders` طلباتي · `/help` الدعم · `/cancel` إلغاء · `/id` معرّفي | `/admin` لوحة الإدارة (أو زر 🛠️ في القائمة) |

---

## 7) مشاكل شائعة

| العرض | السبب والحل |
|---|---|
| السجل يقول `❌ لا يمكن تشغيل البوت — أصلح متغيرات البيئة` | متغير ناقص — الرسالة تسمّيه بالضبط |
| `تيليغرام رفض التوكن` | انسخ التوكن كاملاً من BotFather بلا مسافات |
| `تعذّر الاتصال بقاعدة البيانات` | استخدم رابط **Pooled** من Neon، وتأكد أن المشروع غير متوقف (Neon يوقف المشاريع الخاملة أسابيع) |
| البوت لا يرد أبداً على Render | تأكد `MODE=webhook` و`WEBHOOK_SECRET` موجود؛ افتح `https://promo-bot.onrender.com/health` — إن لم تفتح فالخدمة نائمة أو فشل البناء |
| يرد بعد 30 ثانية من أول رسالة | الخدمة كانت نائمة — فعّل UptimeRobot |
| رسالة `Conflict: terminated by other getUpdates` محلياً | نسختان تعملان polling بنفس التوكن — أغلق إحداهما |

---

## الخطوات القادمة

- ~~2. الرصيد والشحن~~ ✅
- ~~3. معالج إعلان Meta كاملاً + التسعير~~ ✅ (وضع محاكاة)
4. عميل Nour Ads API الحقيقي + المزامنة والمصالحة + تنبيه رصيد نور < 50$
5. تيليغرام (Telegram Ads + القنوات الشريكة) + المجدول والإشعارات
6. التصميم والكتابة، التذاكر، الإحصائيات، البث

---

<p align="center"><sub>صُمِّم وبُني بإبداع مع <b>Arena.ai</b> ✨</sub></p>


## 🆕 ما الجديد في الخطوة 2 (v0.2.3) — الرصيد والشحن

- 💰 **رصيدي**: الرصيد الحالي + آخر عملية + سجل العمليات (صفحات).
- ➕ **شحن رصيد بـ USDT** على شبكتي **TRC20** و **BEP20** فقط — الحد الأدنى 5$.
- بطاقة تعليمات لكل طلب برقم `#TOP-xx` وعنوان قابل للنسخ بضغطة، ثم إثبات (صورة أو TxID).
- 📥 **الأدمن** يستلم بطاقة لكل طلب: ✅ اعتماد / ❌ رفض (أسباب جاهزة) / ✏️ اعتماد بمبلغ مختلف / 💬 مراسلة.
- الاعتماد = معاملة واحدة (رصيد + دفتر `ledger` + حالة الطلب) مع قفل صف — لا يمكن اعتماد الطلب مرتين ولو ضغط أدمنان معاً.
- 🏦 **عناوين المحافظ تُدار من البوت**: 🛠️ لوحة الإدارة → ⚙️ إعدادات → 🏦 طرق الدفع → اختر الشبكة → ✏️ تعديل العنوان.
  **الطريقة لا تظهر للعملاء إلا إذا كانت مفعّلة ولها عنوان.**
- الترحيل `002_topups.sql` يُطبَّق تلقائياً عند الإقلاع (لا حاجة لأي أمر يدوي على Neon).

### تجربة سريعة بعد النشر
1. من حساب الأدمن: `/admin` → ⚙️ إعدادات → 🏦 طرق الدفع → TRC20 → ✏️ تعديل العنوان → ألصق عنوانك.
2. من أي حساب عميل: 💰 رصيدي → ➕ شحن رصيد → TRC20 → 5$ → ✅ حوّلت → أرسل أي صورة.
3. تصلك بطاقة الطلب كأدمن → ✅ اعتماد → يصل العميل «تم شحن 5$» ويظهر الرصيد في 💰 رصيدي.


## 🆕 ما الجديد في الخطوة 3 (v0.3.0) — معالج إعلان فيسبوك/إنستغرام (وضع محاكاة 🧪)

**التسعير النهائي:** سعر العميل = الميزانية × **1.30** بالضبط (منزلتان، بلا حد أدنى) · تكلفتنا عند نور = الميزانية × 1.10 · الربح = 20% من الميزانية.

| الباقة | الميزانية | السعر | تكلفة نور | ربحك |
|---|---|---|---|---|
| 🚀 تجربة | 2$ × 5 أيام = 10$ | **13$** | 11$ | 2$ |
| 📈 نمو | 3$ × 7 أيام = 21$ | **27.30$** | 23.10$ | 4.20$ |
| 💼 احتراف | 5$ × 10 أيام = 50$ | **65$** | 55$ | 10$ |
| 🛠️ مخصص | من 2$/يوم × 1–30 يوماً | ×1.30 | ×1.10 | 20% |
| 📦 انطلاقة متجر | نمو + نص إعلاني + تصميم صورة | **38$** (بدل 40.30$) | 23.10$ | 14.90$ |

**رحلة العميل (7 خطوات):** الباقة → المنصة (فيسبوك / إنستغرام / كلاهما من 4$ يومياً) → الهدف → الدولة (22 دولة) والمحافظات السورية (14) → الجنس والعمر → الرابط + الوصف + حتى 3 ملفات + خيار «نص إعلاني +5$» → واتساب (مع رمز الدولة، والأرقام المحلية 09… تُكمَّل تلقائياً) → **الملخص والتأكيد** (الخصم يحدث هنا فقط، في معاملة مقفلة واحدة).

- **معرّف تيليغرام:** إن لم يكن للعميل @username تظهر شاشة إرشاد بزر «✅ ضبطته — تحقق»؛ وإن تابع بدونه يُرسل **معرّفك الاحتياطي** لنور (🛠️ لوحة الإدارة → ⚙️ إعدادات → 👤 المعرّف الاحتياطي) وتُعلَّم البطاقة «⚠️ بلا معرّف».
- **رصيد ناقص:** يُحفظ الطلب كمسودة `#ORD-xx` (صالحة 7 أيام)، ويظهر زر «➕ اشحن X$ وأكمل» يقترح المبلغ الناقص بالضبط، ثم «📦 أكمل طلبي المعلّق» في الرصيد والباقات.
- **دورة الحياة:** 🟡 قيد المراجعة → 🔵 قيد التجهيز → 🟢 يعمل → ✅ مكتمل، أو ⏸️ متوقف، أو ❌ مرفوض (**استرداد كامل تلقائي**). كل تغيير يصل العميل برسالة، ومع الاكتمال زر «🔁 إعادة الطلب بنفس الإعدادات».
- **بطاقة الأدمن** لكل طلب: السعر/التكلفة/الربح، الاستهداف، الرابط والوصف، 📎 ملفات العميل، 💬 مراسلة العميل، 🔄 مزامنة، ↩️ استرداد كامل مع سبب يصل العميل.
- **🧪 وضع المحاكاة (`NOUR_DRY_RUN=1`):** لا يُرسل شيء لنور فعلياً؛ الأدمن يحاكي مسار نور من البطاقة: ▶️ قَبِل → 🟢 انطلق → ✅ اكتمل / ❌ رفض. هكذا تجرّب المسار كاملاً بدون صرف دولار واحد.
- **حلقة خلفية داخل نفس العملية** (كل 5 دقائق): إعادة إرسال الطلبات العالقة، إلغاء المسودات المنتهية، ومزامنة الحالات مع نور (تعمل في الخطوة 4 حين يصبح `NOUR_DRY_RUN=0`).
- الترحيل `004_orders.sql` يُطبَّق تلقائياً عند الإقلاع.

### تجربة سريعة بعد النشر
1. تأكد أن رصيد حساب العميل التجريبي ≥ 13$ (اعتمد شحنة من لوحة الأدمن).
2. 📢 إعلان فيسبوك / إنستغرام → 🚀 تجربة → فيسبوك → رسائل → سوريا → دمشق → تم → التالي → أرسل رابطاً → وصفاً → تخطّي → لا → رقم واتساب → ✅ صحيح → **✅ تأكيد الطلب — 13$**.
3. تصلك بطاقة `#ORD-1` كأدمن → اضغط 🧪 ▶️ ثم 🧪 🟢 ثم 🧪 ✅ — وراقب رسائل العميل و «📦 طلباتي».
4. جرّب 🧪 ❌ على طلب آخر: يعود المبلغ كاملاً للعميل فوراً، ويظهر في 📜 سجل العمليات.


## 🆕 v0.3.1 — 📡 قنوات الإدارة (حتى لا تختلط الأمور في خاصّك)

| القناة | ماذا يصل إليها |
|---|---|
| 📥 **قناة الإيداعات** | بطاقة كل شحنة `#TOP-xx` بأزرارها (✅ ❌ ✏️ 💬) — وتتحدّث البطاقة بالنتيجة فتصبح القناة سجلاً دائماً |
| 📦 **قناة الطلبات** | بطاقة كل طلب `#ORD-xx` بأزرارها (🧪 / 📎 / 💬 / ↩️) — وتتحدّث مع كل تغيير حالة |
| 🔔 **قناة التنبيهات** | إشعار الإقلاع، الأخطاء غير المتوقعة، الطلبات العالقة، فروق الخصم، وتنبيه رصيد نور (الخطوة 4) |
| 💬 **خاصّك** | لوحة الإدارة `/admin` + كل ما يحتاج كتابة (سبب رفض، مبلغ مختلف، رسالة للعميل) |

**الربط (دقيقة لكل قناة، بلا أرقام):** أنشئ قناة خاصة → المشرفون → أضف البوت بصلاحية «نشر الرسائل» → تصلك في خاصّك رسالة «أُضفت أدمن في قناة جديدة» بثلاثة أزرار → اضغط الغرض. (احتياط: أعد توجيه أي رسالة من القناة إلى البوت.) الإدارة من: 🛠️ → ⚙️ إعدادات → **📡 قنوات الإدارة** (اختبار / فصل).

**قواعد:** الأزرار داخل القناة تستجيب فقط لمن في `ADMIN_IDS`. إن فُصل البوت من قناة أو فقد صلاحيته، تُفصل تلقائياً وتعود البطاقات إلى خاصّك — لا تضيع بطاقة أبداً. القناة غير المربوطة = السلوك القديم (الخاص).


## 🆕 v0.4.0 — 📣 إعلان Telegram Ads الرسمي (تنفيذ يدوي من حسابك)

لا يحتاج أي API: أنت تنشئ الإعلان من حسابك في Telegram Ads، والبوت يدير كل ما حوله.

**العميل (5 خطوات):** الميزانية 10–500$ (السعر = ×1.35 يظهر فوراً) → الاستهداف (📡 قنوات محددة حتى 10 / 🏷️ اهتمامات حتى 5 / 🌐 دولة + لغة / 🤷 خبرة الفريق) → النص ≤160 حرفاً مع عدّاد (أو ✍️ اكتبولي +5$) → الرابط داخل تيليغرام فقط (@قناة / t.me/… — المواقع الخارجية تُرفض بتنبيه) → الملخص والتأكيد (رصيد ناقص = مسودة + «اشحن الفرق»، نفس آلية Meta).

**أنت (من بطاقة الطلب في قناة الطلبات أو خاصّك):** النص قابل للنسخ بضغطة → **▶️ أنشأته** → **🟢 وافق تيليغرام — انطلق** → **✅ اكتمل — أدخل النتائج** (`المشاهدات النقرات` → تصل العميل). وإذا رفض تيليغرام النص: **✏️ اطلب تعديل النص** (سبب يصل العميل → يرسل نصاً جديداً من زر → تتحدّث البطاقة). **❌ رفض تيليغرام** = استرداد كامل تلقائي مع السبب. إن طلب العميل «اكتبولي النص» يظهر لك زر **✍️ أدخل النص الذي كتبته**.

**الحالات:** 🟡 قيد المراجعة → 🔵 أُنشئ (بمراجعة تيليغرام) → 🟢 يعمل → ✅ مكتمل + 📊 نتائج · ✏️ بانتظار تعديلك · ⏸️ متوقف · ❌ مرفوض — مُسترد.
**📝 القنوات الشريكة:** الزر ظاهر مقفولاً «قريباً 🔒» حتى التسليم التالي (إدارة القنوات من البوت).
الترحيل `005_tg_ads.sql` يُطبَّق تلقائياً. مدة المراجعة المعروضة للعميل تُعدَّل من `settings.tg_ads_review_hours`.


## 🆕 v0.5.0 — 🖥️ Cpanel (Mini App داخل تيليغرام) + ✏️ «اكتب مبلغاً آخر»

**ما هي:** لوحة إدارة كاملة تُفتح داخل تيليغرام من زر «🖥️ فتح Cpanel ↗» في لوحة الإدارة، أو بأمر `/cpanel`، أو من زر القائمة ≡ (للأدمن فقط).
تعيش على نفس خدمة Render (`/cpanel`) — **0$، بلا خدمة جديدة، بلا متغيرات بيئة جديدة، بلا BotFather**.

**الأمان:** تيليغرام يرسل مع كل فتح توقيعاً مشفّراً بمفتاح البوت (initData). السيرفر يتحقق منه ومن عمره (ساعة) ومن أن الرقم ضمن `ADMIN_IDS`. من فتح الرابط من متصفح عادي أو ليس أدمن يرى بوابة فقط — لا بيانات.

**التبويبات:**
- 📊 **الرئيسية** — مستخدمون، طلبات، إيراد، صافي ربح وهامش، رسم 7 أيام، «بانتظارك»، الطلبات حسب الحالة، رصيد Nour مقابل أرصدة العملاء، أكثر الخدمات طلباً (اليوم / 7 / 30 / الكل).
- 💲 **الأسعار** — مضاعف Meta وعمولة Nour والحدود، الباقات (تعديل/إخفاء/إضافة حتى 6)، «انطلاقة متجر»، Telegram Ads (مضاعف، حدود، الأرقام الجاهزة)، القنوات الشريكة، الإضافات، التعليق الصوتي، باقات الإضافات، حدود الشحن وأرقامه الجاهزة — **معاينة حية** قبل الحفظ + «↺ الافتراضي» + تحقق يمنع الأسعار الخاسرة.
- 💳 **الدفع** — تشغيل/إطفاء كل طريقة، العناوين والحسابات.
- 🧩 **الخدمات** — مفتاح لكل زر بالقائمة الرئيسية، اختيار «يبقى بشارة قريباً» أو «يختفي»، **وضع صيانة** برسالة تظهر للعميل عند محاولة الطلب.
- ⚙️ **المزيد** — قنوات الإدارة مع «🧪 اختبار»، رابط قناة العروض، معرّف الدعم، مدة مراجعة تيليغرام المعروضة، حد تنبيه Nour، صلاحية المسودة، نص SLA للشحن، المشرفون (للعرض)، **سجل التغييرات** (ماذا/من/متى).

**كيف تسري التغييرات:** كل شيء يُحفظ في جدول `settings` على Neon؛ البوت يقرأ الأسعار الحية من `settings.pricing` (تُحمَّل عند الإقلاع، بعد كل حفظ، وكل 5 دقائق). الطلبات المدفوعة تحتفظ بسعرها وتكلفتها لحظة الدفع.

**✏️ اكتب مبلغاً آخر:** زر واضح تحت الأرقام الجاهزة في ميزانية تيليغرام، وميزانية Meta اليومية («✏️ ميزانيتي بنفسي»)، ومبلغ الشحن.

الترحيل `006_cpanel.sql` يُطبَّق تلقائياً (يضبط حد تنبيه Nour على 50$ ويبذر إعدادات Cpanel).
