# ترويج بوت: نسخة تجريبية بعد المرحلتين 1 و2 من الإصلاحات

الفرع: `fix/phase2-visible-bugs` (يحتوي المرحلة 1 أيضاً)، والإصدار الأساسي 0.9.1.

## ⚙️ قبل التشغيل
- **قاعدة البيانات:** هناك migration جديد اسمه `app/db/migrations/012_idempotency.sql` يُطبَّق **تلقائياً** عند الإقلاع.
  - يضيف عمودين وجدولاً فقط، ولا يعدّل البيانات الحالية ولا يحذف منها شيئاً.
  - ننصح بأخذ نسخة احتياطية من Neon قبل النشر، أو بالتجربة على فرع قاعدة (Neon branch) أولاً.
- **الإعدادات:** متغيرات البيئة هي نفسها، ولا يوجد إعداد جديد.
- **حد الشحن:** اعتماد شحنة بمبلغ معدَّل صار يلتزم بالحد `max_topup_usd` المضبوط من Cpanel (القيمة الافتراضية 1000$).

## ✅ ما الذي تغيّر

### المرحلة 1: منع الخصم المزدوج
| قبل | بعد |
|---|---|
| ضغطتان سريعتان على «تأكيد» تنشئان طلبين وتخصمان مرتين | طلب واحد، والضغطة الثانية ترى «✅ طلبك مسجّل مسبقاً — لم يُخصم أي مبلغ إضافي» |
| تأكيد القناة الشريكة والتصميم قد يعلق (deadlock) | يعمل بلا تعليق |
| زر شراء باقة قديم يشتري مرة أخرى | اشتراك واحد فقط، والزر القديم من الإصدار السابق يعيد فتح شاشة المراجعة |
| زر «تأكيد تعديل الرصيد» عند الأدمن يُطبَّق مرتين | يُطبَّق مرة واحدة، والتكرار يعطي «نُفّذ مسبقاً» |

### المرحلة 2: الأعطال الظاهرة
- **زر تشغيل/إيقاف طرق الدفع:** كان ينهار، والآن يعمل.
- **أسماء العملاء التي فيها `<` أو `&`:** رسالة الترحيب كانت تفشل، والآن تظهر سليمة.
- **البث (الرسالة الجماعية):**
  - يظهر النص كما في المعاينة تماماً؛
  - يُرسَل في الخلفية، فيصلك تقرير عند الانتهاء؛
  - لا يمكن تشغيل بثّين معاً؛
  - يحترم حد الإرسال في تيليغرام (429).
- **اعتماد شحنة بمبلغ مختلف:**
  - يُرفض أي مبلغ أكبر من حد الشحن؛
  - إذا اختلف المبلغ عن طلب العميل بأكثر من 50% يظهر تأكيد إضافي.
- **إصلاحات صغيرة:** إزالة الرد المزدوج في شاشة تشغيل/إيقاف الخدمات، وحذف كود ميت.

## 🧪 قائمة تجربة يدوية على تيليغرام (نحو 15 دقيقة)
1. اطلب إعلان Meta واضغط «تأكيد» **مرتين بسرعة**. المتوقع: طلب واحد وخصم واحد في «سجل الرصيد».
2. كرر ذلك مع Telegram Ads ومع قناة شريكة ومع تصميم.
3. اشترِ باقة مجدولة واضغط التأكيد مرتين. المتوقع: اشتراك واحد.
4. من الأدمن: افتح مستخدماً ثم «➕ إضافة» ثم `5 تجربة`، واضغط التأكيد مرتين. المتوقع: +5 فقط.
5. من الأدمن: افتح طرق الدفع وشغّل/أوقف أي طريقة. المتوقع: يتبدل الزر بلا خطأ.
6. غيّر اسمك في تيليغرام إلى `<سامر & شركاه>` ثم أرسل `/start`. المتوقع: ترحيب سليم.
7. من الأدمن: أرسل بثاً بالنص `عرض <3 & خصم {name}` إلى شريحة صغيرة. المتوقع: النص يصل حرفياً، ثم يصلك تقرير.
8. اطلب شحن 10$ من حساب عميل، ثم من الأدمن اختر «تعديل المبلغ»:
   - اكتب `1000000`: يجب أن يُرفض؛
   - اكتب `30`: يجب أن يطلب تأكيداً؛
   - اكتب `9`: يُعتمد مباشرة.

## 🔬 تشغيل الاختبارات الآلية محلياً (اختياري)
```bash
bash tests/_sim/explore/setup_env.sh          # venv + PostgreSQL محلي (Debian/Ubuntu)
TEST_DATABASE_URL=postgresql://postgres@127.0.0.1:5433/promobot \
BOT_TOKEN=1:x DATABASE_URL=postgresql://u:p@h/d ADMIN_IDS=1 /tmp/venv/bin/python -m pytest -q tests/
/tmp/freshdb.sh && cd tests/_sim/explore && /tmp/venv/bin/python scenarios.py   # السيناريوهات
/tmp/freshdb.sh && /tmp/venv/bin/python crawler.py 500                           # الاستكشاف الآلي
/tmp/freshdb.sh && /tmp/venv/bin/python extra.py                                 # Cpanel والبث والجدولة
```
آخر نتيجة:
- 46/46 اختباراً ناجحاً؛
- 0 مشاكل حرجة؛
- 0 فروقات بين الرصيد والسجل المالي؛
- 10/10 من ملفات المحاكاة الأصلية ناجحة.

## ⏭️ ما لم يُصلَح بعد (المراحل 3–5)
- **التحقق من المدخلات:**
  - صيغة عنوان المحفظة TRC20 ضعيفة؛
  - Cpanel يقبل سعر تعليق صوتي = 0؛
  - مبلغ الشحن يقبل 5.555؛
  - ميزانية Meta تقبل الأرقام العربية بلا توحيد.
- **دفعة الجدولة:** عند التأخر تُرسل عدة رسائل مجدولة دفعة واحدة.
- تفاصيل البنود في `tests/_sim/explore/FIX_PLAN.md`، والتقرير الكامل في `tests/_sim/explore/REPORT.md`.
