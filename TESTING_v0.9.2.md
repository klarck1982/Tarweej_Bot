# ترويج بوت v0.9.2: النسخة النهائية بعد المراحل الخمس من الإصلاحات

الفرع: `fix/phase3-5` (يحتوي كل المراحل 1–5)، والإصدار الأساسي 0.9.1.

## ⚙️ قبل التشغيل
- **قاعدة البيانات:** هناك migration جديد اسمه `app/db/migrations/012_idempotency.sql` يُطبَّق **تلقائياً** عند الإقلاع.
  - يضيف عمودين وجدولاً فقط، ولا يعدّل البيانات الحالية ولا يحذف منها شيئاً.
  - ننصح بأخذ نسخة احتياطية من Neon قبل النشر، أو بالتجربة على فرع قاعدة (Neon branch) أولاً.
- **الإعدادات:** متغيرات البيئة هي نفسها، ولا يوجد إعداد جديد.
- **حد الشحن:** اعتماد شحنة بمبلغ معدَّل صار يلتزم بالحد `max_topup_usd` المضبوط من Cpanel (القيمة الافتراضية 1000$).

## ✅ ما الذي تغيّر

### المرحلة 1: منع الخصم المزدوج
| قبل | بعد |
|---|---|
| ضغطتان سريعتان على «تأكيد» تنشئان طلبين وتخصمان مرتين | طلب واحد، والضغطة الثانية ترى «✅ طلبك مسجّل مسبقاً — لم يُخصم أي مبلغ إضافي» |
| تأكيد القناة الشريكة والتصميم قد يعلق (deadlock) | يعمل بلا تعليق |
| زر شراء باقة قديم يشتري مرة أخرى | اشتراك واحد فقط، والزر القديم من الإصدار السابق يعيد فتح شاشة المراجعة |
| زر «تأكيد تعديل الرصيد» عند الأدمن يُطبَّق مرتين | يُطبَّق مرة واحدة، والتكرار يعطي «نُفّذ مسبقاً» |

### المرحلة 2: الأعطال الظاهرة
- **زر تشغيل/إيقاف طرق الدفع:** كان ينهار، والآن يعمل.
- **أسماء العملاء التي فيها `<` أو `&`:** رسالة الترحيب كانت تفشل، والآن تظهر سليمة.
- **البث (الرسالة الجماعية):**
  - يظهر النص كما في المعاينة تماماً؛
  - يُرسَل في الخلفية، فيصلك تقرير عند الانتهاء؛
  - لا يمكن تشغيل بثّين معاً؛
  - يحترم حد الإرسال في تيليغرام (429).
- **اعتماد شحنة بمبلغ مختلف:**
  - يُرفض أي مبلغ أكبر من حد الشحن؛
  - إذا اختلف المبلغ عن طلب العميل بأكثر من 50% يظهر تأكيد إضافي.
- **إصلاحات صغيرة:** إزالة الرد المزدوج في شاشة تشغيل/إيقاف الخدمات، وحذف كود ميت.

### المرحلة 3: التحقق من المدخلات
- **عناوين المحافظ:**
  - TRC20 يجب أن يبدأ بـ `T` وطوله 34، وBEP20 يبدأ بـ `0x` ويليه 40 رمزاً؛
  - يُطبَّق ذلك في البوت وCpanel معاً؛
  - في البوت تظهر **شاشة مراجعة** قبل الحفظ.
- **المبالغ:** منزلتان عشريتان كحد أقصى. `5.555` و`1e3` و`NaN` مرفوضة، وكانت تُقرَّب بصمت.
- **أسعار Cpanel:** لا خدمة مدفوعة بسعر 0 (الحد الأدنى 1$)، والسعر «قبل الخصم» يجب أن يكون ≥ السعر.
- **سعر صرف الليرة:** يطلب تأكيداً إذا تغيّر أكثر من 30%، لأن ذلك غالباً صفر ناقص أو زائد.

### المرحلة 4: المتانة
- **التسليمات المجدولة المتأخرة:** تصل **واحداً كل ساعة** بدل دفعة من 7 رسائل، مع تنبيه للأدمن بعدد المتأخر.
- **فشل الإرسال المؤقت:** يُعاد بعد 10 ثم 20 دقيقة بدل 3 محاولات في ثوانٍ، **مع الحفاظ على ترتيب التصاميم**.
- **العميل الذي حظر البوت:** تتوقف باقته فوراً، ويصل الأدمن تنبيه واضح.
- **إلغاء الاشتراك:** أخذ نفس ترتيب الأقفال، فلا يعلق عند التزامن مع شراء.
- **ملفات المشروع:** أُضيف `.env.example` (كانت رسالة الخطأ تشير إليه دون أن يوجد)، و`.gitignore` كامل، و`pytest` يعمل الآن بلا إعداد.
- **README:** حُدّث إلى v0.9.2، مع قسم للباقات المجدولة.

### المرحلة 5: التحقق
- **اختبارات سباق دائمة (`tests/test_races.py`):**
  - اعتماد الشحن المزدوج، والاعتماد ضد الرفض؛
  - الاسترداد المزدوج؛
  - 5 طلبات متزامنة مع رصيد يكفي لطلبين فقط؛
  - إلغاء متزامن مع شراء؛
  - عاملان للمجدول معاً؛
  - مهلة الساعة وترتيب التسليم.
- **إجمالي الاختبارات:** 78.

## 🧪 قائمة تجربة يدوية على تيليغرام (نحو 20 دقيقة)
1. اطلب إعلان Meta واضغط «تأكيد» **مرتين بسرعة**. المتوقع: طلب واحد وخصم واحد في «سجل الرصيد».
2. كرر ذلك مع Telegram Ads ومع قناة شريكة ومع تصميم.
3. اشترِ باقة مجدولة واضغط التأكيد مرتين. المتوقع: اشتراك واحد.
4. من الأدمن: افتح مستخدماً ثم «➕ إضافة» ثم `5 تجربة`، واضغط التأكيد مرتين. المتوقع: +5 فقط.
5. من الأدمن: افتح طرق الدفع وشغّل/أوقف أي طريقة. المتوقع: يتبدل الزر بلا خطأ.
6. غيّر اسمك في تيليغرام إلى `<سامر & شركاه>` ثم أرسل `/start`. المتوقع: ترحيب سليم.
7. من الأدمن: أرسل بثاً بالنص `عرض <3 & خصم {name}` إلى شريحة صغيرة. المتوقع: النص يصل حرفياً، ثم يصلك تقرير.
8. اطلب شحن 10$ من حساب عميل، ثم من الأدمن اختر «تعديل المبلغ»:
   - اكتب `1000000`: يجب أن يُرفض؛
   - اكتب `30`: يجب أن يطلب تأكيداً؛
   - اكتب `9`: يُعتمد مباشرة.
9. من الأدمن: افتح طرق الدفع ثم USDT TRC20 ثم «تعديل»، والصق عنوان BEP20 (يبدأ بـ 0x).
   - المتوقع: رفض مع شرح الصيغة؛
   - ثم الصق عنوانك الحقيقي: تظهر شاشة مراجعة، فاضغط «✅ صحيح — احفظه».
10. من حساب عميل: اشحن بمبلغ `5.555`. المتوقع: رفض مع رسالة واضحة.
11. من Cpanel: ضع سعر «التعليق الصوتي» = 0 ثم احفظ. المتوقع: رسالة خطأ ولا يُحفظ.

## 🔬 تشغيل الاختبارات الآلية محلياً (اختياري)
```bash
bash tests/_sim/explore/setup_env.sh          # venv + PostgreSQL محلي (Debian/Ubuntu)
TEST_DATABASE_URL=postgresql://postgres@127.0.0.1:5433/promobot \
BOT_TOKEN=1:x DATABASE_URL=postgresql://u:p@h/d ADMIN_IDS=1 /tmp/venv/bin/python -m pytest -q tests/
bash tests/_sim/explore/run_all.sh 500          # السيناريوهات + الاستكشاف الآلي + Cpanel/البث/الجدولة
```
آخر نتيجة (بعد المرحلة 5):
- **الاختبارات:** 78/78 ناجحة، و59 منها تعمل بلا قاعدة ولا إعداد.
- **المحاكاة:** **0 مشاكل** بكل المستويات (حرجة، عالية، متوسطة، منخفضة):
  - الاستكشاف الآلي 800 خطوة (1867 ضغطة وإدخالاً)؛
  - 11 سيناريو؛
  - Cpanel والبث والجدولة.
- **السجل المالي:** 0 فروقات بين الرصيد والسجل.
- **المحاكاة الأصلية للمشروع:** 10/10 ناجحة.

## 🚀 خطوات النشر المقترحة
1. **نسخة احتياطية:** في Neon اختر Branches ثم أنشئ `dev` من `main`. هذه نسخة كاملة للتجربة.
2. **بوت تجريبي:** أنشئ بوتاً ثانياً من BotFather، وشغّل النسخة محلياً (`MODE=polling`) مع رابط فرع `dev`.
   - أو أنشئ خدمة Render ثانية بنفس الإعدادات.
3. **التجربة اليدوية:** نفّذ القائمة أعلاه (نحو 20 دقيقة).
4. **النشر:** ارفع الملفات إلى المستودع الأساسي، فيعيد Render النشر تلقائياً. الترحيل `012` يُطبَّق وحده عند الإقلاع.
5. **بعد النشر:** راقب سجلات Render أول ساعة، وتأكد أن `/health` يعيد 200.

## ⏭️ ملاحظات للمستقبل (ليست أعطالاً)
- **رقم حساب شام كاش:** يُقبل بأي صيغة من أحرف وأرقام، لأن صيغته الرسمية غير موحّدة. الحماية هنا شاشة المراجعة قبل الحفظ.
- **الأزرار القديمة:** أزرار تأكيد الطلبات المرسلة قبل التحديث تعيد فتح شاشة المراجعة بدل الشراء المباشر، وهذا مقصود.
- **تنبيه التأخر:** يُرسل مرة واحدة لكل اشتراك في كل تشغيل للخادم.
